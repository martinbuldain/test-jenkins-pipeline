pipeline {
    agent any
    stages {
        stage ('Build') {
            steps {
                script {
                    sh "git ls-files"
                    echo 'Printing output'
                }
            }   
        }
    }
}